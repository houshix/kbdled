# kbdled
Linux tool to toggle a generic keyboard's Scroll Lock LED with a dedicated key — even on the SDDM/GDM login screen. Single static Go binary, zero dependencies, 6-language interactive wizard.
